# The Men's Code

Responsive storefront with high-resolution campaign artwork and outlined SVG logos. Price: IDR 98,000. Buy buttons open recipient/address entry. Draft data stays on the shopper's device; order submission is disabled until shipping/payment APIs and persistent storage are configured.

Run npm start. Run npm test. ORDERS_ENABLED must remain false until order processing is intentionally launched.
